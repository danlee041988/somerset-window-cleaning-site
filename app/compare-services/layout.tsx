export const metadata = {
  title: 'Compare Services & Build Packages | Somerset Window Cleaning',
  description: 'Compare our exterior cleaning services and build custom packages. Save 10-20% with service bundles. Interactive comparison tool to find your perfect maintenance plan.',
  keywords: 'service comparison, cleaning packages Somerset, bundle deals, service packages, maintenance plans'
}

export default function CompareServicesLayout({
  children,
}: {
  children: React.ReactNode
}) {
  return children
}